# VAmPI Generalized Mission Actor Script

- **Cut length:** `02:58`
- **Voice:** [spoken transcript](./VOICE_TRANSCRIPT.md)
- **Captions:** [SRT subtitles](./subtitles.srt)
**Run evidence:** [`vampi-real-mission-history-2026-07-20.json`](../../../examples/run-exports/vampi-real-mission-history-2026-07-20.json)

This is the recommended competition cut because it demonstrates that Reality
Engine is a generalized counterfactual runtime rather than a scripted
password-reset visualization. This file contains stage directions only; do not
read them aloud.

## Before The Take

1. Open the stabilised `VAmPI Ownership Regression` run from Mission Control at
   `1440x1000` or a 16:10 desktop crop.
2. Confirm the outcome shows 14 Dreams lived, depth 3, 44 Subjects, 14 admitted
   Memories, one detected intervention, one passing proof, and seven changed
   files.
3. Expand every graph branch. Confirm 15 Reality nodes are readable and Subject
   cards remain beneath their owning Dream.
4. Load all earlier events before the timer starts so the root GPT-5.6 and
   Subject milestones are replayable. The Playwright actor does this
   automatically.
5. Keep the browser at 100% zoom, close DevTools, and never show credentials,
   raw Subject messages, or hidden reasoning.

## Stage Directions

| Time | Actor direction - not spoken | Proof held on screen |
| --- | --- | --- |
| `00:00` | **Hold.** Start at the top of the stabilised outcome. | 14 Dreams, depth 3, 44 Subjects, 14 Memories, one detected fault, one passing proof, seven changed files. |
| `00:17` | Scroll to **Realities**, then drag the topology toward Level 3 at `00:22`. | One ROOT plus 14 nested Dream nodes across three levels; horizontal panning preserves the inspector at right. |
| `00:28` | Rewind the sticky timeline to the first milestone. | Only ROOT exists before exploration begins. |
| `00:38` | Select the root `gpt-5.6-sol` milestone and click the eye icon. **Hold.** | GPT-5.6-sol, Codex SDK, CLI auth, persisted thread, and isolated worktree. |
| `00:54` | Close the event dialog. | Clean return to the replay. |
| `00:57` | Advance to `Enforce ownership at the book handler boundary`. | The first counterfactual separates from the protected repository. |
| `01:09` | Advance to `Branch-only counterfactual replay`. | A Dream inside a Dream appears without changing its parents. |
| `01:18` | Advance to Mal entering and click the timeline eye icon. **Hold.** | Native Adversarial Subject thread and bounded collaboration evidence. |
| `01:34` | Close the Subject event dialog. | Return to an unobstructed replay workspace. |
| `01:36` | Advance through the final Eames `Subject entered` milestone in `Fail-closed identity and role matrix`, then pan to Level 3 at `01:38`. | The replay exposes the complete depth-three graph with Subjects beneath every owning Dream. |
| `01:43` | Jump to `Adversarial fault contained` and scroll to **Totem Check**. | One injected path restored before crossing the Kick boundary. |
| `02:04` | Advance to the late parent Memory return and scroll to **Memory Ascent**. | Verified Memory travels child to parent; unsafe mutation does not. |
| `02:13` | Advance to the final `Reality Mirror compared` event and scroll to **Reality Mirror**. | Sibling evidence, shared invariants, and retained disagreements. |
| `02:24` | Advance to `Reality stabilised` and pause briefly on the outcome. | The measurable result returns only after the complete replay. |
| `02:30` | Scroll to **Anchor Proof**. | The immutable `python3 tests/test_authorization_regression.py` proof passed 12 tests. |
| `02:37` | Scroll to **Final Git diff**. | Four judge-facing product/test files follow the proof; three engine-owned control files are excluded. |
| `02:39` | Click **Reveal code**. | Owner-qualified lookup, fail-closed identity, administrator deletion, and the retained proof are readable. |
| `02:46` | Click **Mission Control**. Do not launch Codex. | General premises, proofs, Subjects, strategy, depth, and budgets. |
| `02:54` | Return to the stabilised VAmPI run and hold the outcome. | Close on the measurable result and complete provenance. |

## Take Acceptance

- The 15-node topology remains readable after the chosen crop and upload
  compression. If it does not, use the password-reset cut.
- The graph visibly rewinds to ROOT and later expands to depth three.
- One real model event and Mal's native Subject event are readable.
- Totem Check, Memory Ascent, Reality Mirror, Anchor Proof, and final diff appear
  in that causal order.
- No historical quota, validation, or recovered fracture event is opened during
  the take; the preserved event log remains honest and available to judges.
- The final frame lands before `03:00`.
